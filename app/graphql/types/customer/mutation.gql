input UpdateCustomerAuthenticationQuestionsInput {
  referenceNumber: ID!
  memorableDate: String
  memorableEvent: String
  memorablePlace: String
}

extend type Mutation {
  updateCustomerAuthenticationQuestions(
    input: UpdateCustomerAuthenticationQuestionsInput!
  ): CustomerAuthenticationQuestions
}
