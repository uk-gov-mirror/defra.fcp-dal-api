export default {
  // No semicolons (StandardJS style)
  semi: false,
  // Use single quotes (StandardJS style)
  singleQuote: true,
  // No trailing commas (StandardJS style)
  trailingComma: 'none',
  // Use spaces inside object literals (StandardJS style)
  bracketSpacing: true,
  // Use parentheses around arrow function parameters (StandardJS style)
  arrowParens: 'always',
  // StandardJS recommends 80 characters per line (project override)
  printWidth: 100,
  // Use 2 spaces per indentation level
  tabWidth: 2,
  // Use spaces (not tabs) for indentation
  useTabs: false
}
